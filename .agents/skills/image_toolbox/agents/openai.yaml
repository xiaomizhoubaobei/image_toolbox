interface:
  display_name: "Image Toolbox"
  short_description: "Repo-specific patterns and workflows for image_toolbox"
  default_prompt: "Use the image_toolbox repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true