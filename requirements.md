# 功能需求文档

> **说明**: 本文档仅记录待开发的功能规划。

## 导航栏组件 (Navbar)

### 当前状态
- 组件已创建但功能未完整实现
- 仅显示固定在顶部的空白导航栏

### 计划功能

#### 1. Logo 点击返回首页
- **描述**：点击 Logo 图片跳转到首页
- **优先级**：中
- **UI 设计**：
  - Logo 位于导航栏左侧
  - 使用项目 Logo 图片（/favicon.webp）
  - 鼠标悬停时显示指针样式（cursor-pointer）
  - 添加 hover 效果（轻微放大或透明度变化）
- **交互行为**：
  - 点击 Logo 时调用 `router.push('/')`
  - 阻止默认链接行为
- **技术要点**：
  - 使用 `router.push('/')` 实现跳转
  - 使用 Next.js Image 组件优化加载
- **多语言支持**：
  - Logo alt 文本支持国际化

#### 2. 导航菜单
- **描述**：添加主要功能页面的导航链接
- **优先级**：高
- **功能点**：
  - 首页（/）
  - 工具页面（/tools）
  - 帮助/文档（/help）
  - 关于页面（/about）
- **UI 设计**：
  - 水平排列的导航链接
  - 当前页面链接高亮显示（active 状态）
  - 鼠标悬停效果（文字颜色变化、下划线）
  - 响应式间距（桌面端显示完整，移动端折叠）
- **交互行为**：
  - 点击导航项时调用 `router.push(route)`
  - 使用 active 状态标识当前页面
  - 平滑过渡动画
- **技术要点**：
  - 使用 `usePathname` 判断当前路由
  - 路由配置与实际页面路由对应
- **多语言支持**：
  - 所有导航文本支持国际化（使用 Locale）

#### 3. 用户信息区域
- **描述**：完善用户信息显示和操作入口
- **优先级**：中
- **功能点**：
  - 用户头像/名称显示
  - 设置按钮入口
  - 登出功能
- **UI 设计**：
  - 位于导航栏右侧
  - 显示用户头像（圆形，尺寸 32x32）
  - 显示用户名称（可配置显示/隐藏）
  - 下拉菜单展示操作选项
  - 下拉菜单包含：设置、登出选项
- **交互行为**：
  - 点击用户头像/名称展开下拉菜单
  - 点击外部区域关闭下拉菜单
  - 点击设置按钮跳转到设置页面
  - 点击登出按钮调用登出 API
  - 登出后清除用户状态并跳转到登录页
- **技术要点**：
  - 使用 Zustand store 获取用户信息
  - 下拉菜单使用 Radix UI Dropdown Menu
  - 登出功能调用 `/api/auth/logout`
- **多语言支持**：
  - 所有文本支持国际化

#### 4. 移动端响应式
- **描述**：适配移动端显示，支持汉堡菜单
- **优先级**：中
- **功能点**：
  - 移动端折叠导航
  - 汉堡菜单按钮
  - 侧边抽屉式菜单
- **UI 设计**：
  - 移动端（< 768px）显示汉堡菜单按钮
  - 点击汉堡菜单展开侧边抽屉
  - 抽屉从右侧滑出
  - 遮罩层背景
  - 抽屉内包含：导航菜单、用户信息
- **交互行为**：
  - 点击汉堡菜单按钮展开抽屉
  - 点击遮罩层或菜单项关闭抽屉
  - 滑动关闭抽屉（可选）
- **技术要点**：
  - 使用 Radix UI Sheet 组件
  - 响应式断点：768px
  - 平滑过渡动画
- **多语言支持**：
  - 所有文本支持国际化

### 技术要求
- 使用 `next/navigation` 的 `useRouter` 进行路由导航
- 使用 `next/navigation` 的 `usePathname` 判断当前路由
- 使用 Zustand store 管理用户状态
- 使用 Radix UI 组件库构建交互组件
- 保持组件的响应式设计
- 支持多语言国际化（使用 Locale）
- 遵循项目现有的 Tailwind CSS 样式规范

### 注意事项
- 当前 `router` 变量已预留，用于实现上述导航功能
- 不要删除 `router` 相关代码，待功能实现后使用
- 用户信息需要从 store 中获取，确保数据同步
- 移动端抽屉复用现有的 `side-sheet` 组件样式
- 所有跳转使用客户端路由，避免页面刷新

---

## 图片预览功能 (Image Viewer)

### 当前状态
- 组件已创建：`src/components/image-viewer.tsx`
- 组件已实现全屏图片查看功能
- 组件未被集成使用，待接入

### 计划功能

#### 1. 结果图片预览
- **描述**：在工具处理完成后，点击结果图片可以全屏预览
- **优先级**：高
- **UI 设计**：
  - 点击结果图片时显示全屏查看器
  - 查看器背景为半透明白色（bg-white/90）
  - 图片居中显示，最大宽度 100%
  - 底部显示关闭按钮（"close" 文字，可优化为图标）
  - 支持响应式，适配不同屏幕尺寸
- **交互行为**：
  - 点击结果图片打开全屏查看器
  - 点击背景或关闭按钮关闭查看器
  - 点击图片本身阻止事件冒泡，防止误关闭
  - 关闭时清空 `src` 状态
- **技术要点**：
  - 使用现有的 `ImageViewer` 组件
  - 通过 `setSrc('')` 控制显示/隐藏
  - 使用 `stopPropagation` 防止事件冒泡
- **多语言支持**：
  - 关闭按钮文本支持国际化（"关闭"/"Close"）

#### 2. 原始图片预览
- **描述**：支持对上传的原始图片进行全屏预览
- **优先级**：中
- **UI 设计**：
  - 在图片编辑区域添加预览按钮
  - 预览按钮使用图标（如放大镜图标）
  - 按钮位置：图片右上角或右下角
  - hover 效果：按钮透明度变化
- **交互行为**：
  - 点击预览按钮打开全屏查看器
  - 显示当前编辑的原始图片
  - 关闭后返回编辑界面
- **技术要点**：
  - 复用 `ImageViewer` 组件
  - 预览时显示原始 `src`，不显示编辑结果
- **多语言支持**：
  - 预览按钮 tooltip 支持国际化

#### 3. 对比预览模式
- **描述**：支持原图与处理结果的对比预览
- **优先级**：低
- **UI 设计**：
  - 全屏查看器中并排显示原图和结果图
  - 或使用滑块进行拖拽对比（类似 `image-compare` 组件）
  - 提供标签切换：原图 / 结果 / 对比
- **交互行为**：
  - 在查看器中切换显示模式
  - 对比模式下可以直观看到处理前后的差异
- **技术要点**：
  - 复用现有的 `image-compare` 组件逻辑
  - 在全屏模式下集成对比功能
- **多语言支持**：
  - 标签文本支持国际化

### 技术要求
- 使用现有的 `ImageViewer` 组件
- 在 `image-transfer.tsx` 等组件中集成预览功能
- 确保预览时图片质量不损失
- 支持大图片的流畅加载和显示
- 优化移动端预览体验

### 注意事项
- `ImageViewer` 组件已实现，不要删除
- 当前组件的关闭按钮是文字，建议优化为图标
- 考虑添加图片缩放功能（可选）
- 考虑添加图片旋转功能（可选）
- 确保预览不影响当前的编辑状态

---

## 图片编辑组件增强功能

### 图片外扩组件 (Image Uncropper)

#### 显示缩放级别
- **描述**：在图片外扩组件中显示当前缩放级别
- **优先级**：中
- **UI 设计**：
  - 在工具栏中显示缩放级别指示器
  - 位置：缩放按钮附近或工具栏中央
  - 显示格式：百分比（如 "100%"、"120%"、"80%"）
  - 默认显示 "100%"
  - 使用半透明背景，避免遮挡图片
- **交互行为**：
  - 点击放大按钮时，缩放级别更新为 "120%"
  - 点击缩小按钮时，缩放级别更新为 "80%"
  - 点击重置按钮时，缩放级别恢复为 "100%"
  - 缩放级别随操作实时更新
- **技术要点**：
  - 添加 `zoom` 状态：`const [zoom, setZoom] = useState(100)`
  - 在 `handleZoomIn` 中调用 `setZoom(120)`
  - 在 `handleZoomOut` 中调用 `setZoom(80)`
  - 在 `handleReset` 中调用 `setZoom(100)`
  - 在 JSX 中显示：`{zoom}%`
- **多语言支持**：
  - 可选：添加"缩放"标签支持国际化

#### 拖拽式尺寸调整
- **描述**：支持通过拖拽直接调整裁剪框尺寸
- **优先级**：高
- **UI 设计**：
  - 裁剪框四角显示调整手柄
  - 手柄样式：小方块或圆形
  - hover 效果：手柄高亮
  - 拖拽时显示实时尺寸提示
- **交互行为**：
  - 拖拽四角调整裁剪框大小
  - 拖拽四边调整单边尺寸
  - 拖拽时更新宽度/高度输入框
  - 拖拽结束后生成新的遮罩
- **技术要点**：
  - 利用 `react-advanced-cropper` 的拖拽功能
  - 监听 `onChange` 事件更新尺寸
  - 实时同步到 `pixel` 状态

### 图片遮罩组件 (Image Mask)

#### 撤销/重做功能
- **描述**：支持对绘制操作进行撤销和重做
- **优先级**：中
- **UI 设计**：
  - 在工具栏左侧添加撤销按钮（使用 `LiaUndoAltSolid` 图标）
  - 在工具栏左侧添加重做按钮（使用 `LiaRedoAltSolid` 图标）
  - 按钮位置：紧邻清空按钮（`clear` 按钮）
  - 按钮样式：圆形按钮，背景 bg-slate-200
  - hover 效果：hover:text-white hover:bg-violet-500
  - 禁用状态：当无法撤销/重做时，按钮显示灰色（opacity-50）且不可点击
- **交互行为**：
  - 点击撤销按钮：恢复到上一次绘制状态
  - 点击重做按钮：恢复到撤销前的状态
  - 每次鼠标抬起时（`handleMouseUp`）自动记录新的历史状态
  - 清空按钮（`clear`）重置历史记录
  - 历史索引限制：最多保留 50 次历史记录（可选）
- **技术要点**：
  - 使用现有的 `history` 状态：`Line[][]`
  - 使用现有的 `historyIndex` 状态：`number`
  - 撤销逻辑：`setLines(history[historyIndex - 1])` 和 `setHistoryIndex(historyIndex - 1)`
  - 重做逻辑：`setLines(history[historyIndex + 1])` 和 `setHistoryIndex(historyIndex + 1)`
  - 在 JSX 中调用：`onClick={undo}` 和 `onClick={redo}`
  - 添加 `LiaRedoAltSolid` 图标导入：`import { LiaRedoAltSolid } from "react-icons/lia"`
- **多语言支持**：
  - 按钮 tooltip 支持国际化（"撤销"/"Undo"、"重做"/"Redo"）
- **注意事项**：
  - 当前 `undo` 和 `redo` 函数已实现，但未绑定到 UI
  - 需要添加按钮并绑定事件处理器
  - 添加禁用状态判断：`historyIndex === 0` 时禁用撤销，`historyIndex === history.length - 1` 时禁用重做

### 图片裁剪组件 (Image Cropper)

#### 裁剪结果实时预览
- **描述**：在裁剪操作完成后，更新图片源以显示裁剪后的效果
- **优先级**：中
- **UI 设计**：
  - 裁剪操作完成后，自动更新显示区域显示裁剪结果
  - 保持裁剪比例和缩放状态
  - 提供平滑过渡动画
- **交互行为**：
  - 用户调整裁剪框时，实时更新 `payload.canvas`
  - 裁剪完成后，调用 `setSrc(croppedImageUrl)` 更新图片源
  - 用户可以看到裁剪后的实际效果
- **技术要点**：
  - 使用现有的 `setSrc` 参数（当前已预留但未使用）
  - 从 `payload.canvas` 获取裁剪后的 canvas
  - 使用 `canvas.toDataURL()` 转换为图片 URL
  - 在 `onChange` 或 `onReady` 回调中调用 `setSrc`
- **多语言支持**：
  - 可选：添加提示信息支持国际化
- **注意事项**：
  - 当前 `setSrc` 参数已存在，但未实现更新逻辑
  - 需要确保裁剪后的图片质量不损失
  - 考虑添加"恢复原图"功能，让用户可以撤销裁剪操作

---

## 图片编辑组件接口规范

### 组件接口设计原则

#### 只读 vs 可写图片源
- **只读图片源**：组件只读取 `src`，不修改图片源
  - 适用场景：生成遮罩、辅助工具、预览组件
  - 接口：`{ src: string, setPayload: (data: any) => void }`
  - 示例：`ImageUncropper`（外扩）、`ImageMask`（遮罩）

- **可写图片源**：组件可以修改图片源
  - 适用场景：图片编辑、滤镜应用、裁剪操作
  - 接口：`{ src: string, setSrc: (src: string) => void, setPayload: (data: any) => void }`
  - 示例：`ImageEditor`（滤镜编辑）、可能的其他编辑组件

#### 接口一致性
- 同类型组件应保持接口一致
- 不必要的参数应删除，避免接口臃肿
- 组件职责单一化，避免过度设计

### 组件职责划分

#### ImageUncropper（图片外扩）
- **职责**：生成外扩遮罩文件
- **输入**：原始图片 URL
- **输出**：外扩遮罩文件、外扩位置信息
- **不负责**：修改图片源、显示处理结果
- **接口**：`{ src: string, setPayload: (data: any) => void }`
- **变更说明**：已移除 `setSrc` 参数，因为组件不负责更新图片源

#### ImageMask（图片遮罩）
- **职责**：绘制遮罩区域
- **输入**：原始图片 URL
- **输出**：遮罩文件
- **不负责**：修改图片源
- **接口**：`{ src: string, setPayload: (data: any) => void }`

#### ImageEditor（图片编辑器）
- **职责**：应用滤镜和调整参数
- **输入**：原始图片 URL
- **输出**：编辑后的图片、编辑参数
- **负责**：更新图片源显示编辑结果
- **接口**：`{ src: string, setSrc: (src: string) => void, setPayload: (data: any) => void }`

---

## 图片文字识别功能

### Doc2x 文档文字识别
- **描述**：使用 Doc2x 服务识别文档图片中的文字内容
- **优先级**：低
- **状态**：待开发
- **功能点**：
  - 支持上传文档图片文件
  - 使用 Doc2x API 进行文字识别
  - 异步任务处理，支持立即返回或轮询任务状态
  - 返回识别的文字内容
- **UI 设计**：
  - 在工具列表中添加"文档识别"入口
  - 支持拖拽或点击上传文档图片
  - 显示识别进度（如果需要轮询）
  - 识别完成后显示文字内容，支持复制
- **交互行为**：
  - 用户上传文档图片后发起识别请求
  - 如果任务未完成，显示加载状态并轮询任务状态
  - 识别完成后展示识别结果
  - 提供复制、下载等功能
- **技术要点**：
  - 使用 `getDoc2xText` 函数（已实现但未集成）
  - 调用 Doc2x API：`/doc2x/api/v1/async/img`
  - 使用 `fetchDoc2xTask` 轮询任务状态
  - 支持异步任务处理
- **多语言支持**：
  - 界面文本支持国际化
  - 识别结果保持原文语言
- **注意事项**：
  - 当前函数已实现但未在界面中集成
  - 与 `aiImageToText`（GPT-4o）相比，Doc2x 可能更适合文档类图片
  - 考虑在功能描述中区分两种识别方式的适用场景

### 与现有功能对比
| 功能        | 使用技术      | 适用场景     | 集成状态 |
|-----------|-----------|----------|------|
| read-text | GPT-4o    | 通用图片文字识别 | 已集成  |
| Doc2x 识别  | Doc2x API | 文档图片文字识别 | 待开发  |

---

## 新增功能规划

### 批量处理功能 (Batch Processing)

#### 功能描述
支持用户一次性对多张图片进行相同的处理操作，大幅提升处理效率。

#### 优先级
最高 ⭐⭐⭐⭐⭐

#### 功能点
- 批量去背景
- 批量压缩
- 批量格式转换（JPG/PNG/WebP）
- 批量调整尺寸
- 批量应用滤镜

#### UI 设计
- 在着陆页添加"批量处理"入口
- 支持拖拽或点击选择多张图片（最多20张）
- 图片列表展示：缩略图 + 文件名 + 大小
- 支持拖拽调整处理顺序
- 全选/取消全选功能
- 移除单张或批量移除功能
- 选择处理类型（去背景/压缩/转换等）
- 统一的参数设置面板
- 显示处理进度（已处理/总数）
- 处理完成后提供批量下载

#### 交互行为
- 上传图片后显示预览列表
- 选择处理类型后显示对应参数设置
- 点击"开始处理"按钮开始批量处理
- 队列管理：按顺序处理，支持并发控制（最多3个并发）
- 实时更新每张图片的处理状态（等待/处理中/完成/失败）
- 处理完成后高亮显示结果
- 支持单独下载某张图片或批量下载所有结果
- 失败图片支持重试

#### 技术要点
- 使用现有的图片处理 API（image-processing.ts）
- 实现队列管理：`Queue<T>` 数据结构
- 并发控制：限制同时处理的任务数量（最多3个）
- 进度追踪：维护 `completed` / `failed` / `total` 计数
- 压缩打包：使用 JSZip 打包批量下载
- 错误处理：单张失败不影响其他图片处理
- 使用 `Promise.allSettled` 处理并发任务

#### API 端点
复用现有 API：
- `/sd/v2beta/stable-image/edit/remove-background` (去背景)
- `/302/submit/upscale` (放大)
- 等等...

#### 多语言支持
- "批量处理" / "Batch Processing"
- "上传图片" / "Upload Images"
- "开始处理" / "Start Processing"
- "下载全部" / "Download All"
- 状态文本支持国际化

#### 开发周期
2-3 周

---

### 风格迁移功能 (Style Transfer)

#### 功能描述
将用户上传的照片转换为特定艺术风格，支持多种风格选择。

#### 优先级
高 ⭐⭐⭐⭐⭐

#### 功能点
- 油画风格迁移
- 动漫风格迁移
- 像素艺术风格
- 水彩画风格
- 素描风格
- 浮世绘风格

#### UI 设计
- 在工具列表中添加"风格迁移"卡片
- 图标：艺术调色板图标
- 上传原始图片
- 风格选择器：网格展示风格缩略图
- 每个风格显示名称 + 示例图
- 选中风格高亮显示
- 风格强度滑块（0-100）
- 对比模式：原图 vs 风格迁移结果

#### 交互行为
- 用户上传原始图片
- 选择风格后显示风格示例
- 调整风格强度，实时预览（可选）
- 点击"生成"开始风格迁移
- 显示处理进度
- 生成完成后展示对比效果
- 支持重新选择风格重新生成
- 下载或继续编辑

#### 技术要点
- 使用 Stable Diffusion img2img API
- 风格提示词预定义（提示词库）
- 风格强度控制：denoising strength 参数
- 支持自定义风格（高级用户可输入提示词）
- 使用 `image-compare` 组件展示对比效果

#### API 端点
- `/302/submit/stable-diffusion-xl` 或类似 img2img API
- 参数：image, prompt, negative_prompt, denoising_strength

#### 多语言支持
- "风格迁移" / "Style Transfer"
- 风格名称支持国际化
- "风格强度" / "Style Strength"

#### 开发周期
1-2 周

---

### 老照片修复功能 (Old Photo Restoration)

#### 功能描述
修复老照片的划痕、折痕、模糊等问题，与"黑白上色"功能形成完整的老照片处理流程。

#### 优先级
高 ⭐⭐⭐⭐

#### 功能点
- 划痕修复
- 折痕修复
- 模糊增强
- 人脸清晰化
- 噪点去除

#### UI 设计
- 在工具列表中添加"老照片修复"卡片
- 图标：复古相机图标
- 上传老照片
- 修复选项：
  - 划痕修复（开关）
  - 折痕修复（开关）
  - 模糊增强（开关）
  - 人脸增强（开关）
- 修复程度滑块（轻微/标准/强力）
- 支持与"黑白上色"功能联动

#### 交互行为
- 用户上传老照片
- 勾选需要修复的选项
- 调整修复程度
- 点击"开始修复"
- 显示处理进度
- 修复完成后展示对比效果
- 提供一键"上色"按钮（跳转到黑白上色功能）
- 下载或继续编辑

#### 技术要点
- 使用 GFPGAN 或 CodeFormer API
- 支持多种修复模式组合
- 修复程度控制：模型参数调整
- 与"黑白上色"功能联动：传递修复后的图片

#### API 端点
- `/302/submit/gfpgan` 或 `/302/submit/codeformer`
- 参数：image, enhancement_level

#### 多语言支持
- "老照片修复" / "Old Photo Restoration"
- 修复选项支持国际化
- "上色" / "Colorize"

#### 开发周期
1 周

---

### 人像美化功能 (Portrait Enhancement)

#### 功能描述
精细的人像处理功能，与"人物滤镜"功能互补。

#### 优先级
中 ⭐⭐⭐⭐

#### 功能点
- 智能磨皮
- 瘦脸
- 大眼
- 美白
- 妆容迁移

#### UI 设计
- 在工具列表中添加"人像美化"卡片
- 图标：人脸图标
- 上传人像照片
- 人脸检测：自动识别人脸区域
- 美化选项：
  - 磨皮程度（滑块 0-100）
  - 瘦脸程度（滑块 0-100）
  - 大眼程度（滑块 0-100）
  - 美白程度（滑块 0-100）
- 妆容选择器：提供多种妆容模板
- 实时预览效果（可选）

#### 交互行为
- 用户上传人像照片
- 系统自动检测人脸
- 调整各项美化参数
- 或选择妆容模板
- 点击"应用"开始处理
- 处理完成后展示对比效果
- 支持撤销/重做
- 下载或继续编辑

#### 技术要点
- 使用人像美化 API（ArcSoft 或自研）
- 人脸检测：定位人脸关键点
- 参数微调：滑块值映射到 API 参数
- 妆容迁移：使用风格迁移技术
- 实时预览：使用 Debounce 防抖

#### API 端点
- `/302/submit/portrait-enhancement`
- 参数：image, smooth, slim_face, enlarge_eye, whiten

#### 多语言支持
- "人像美化" / "Portrait Enhancement"
- 美化选项支持国际化
- 妆容名称支持国际化

#### 开发周期
2 周

---

### 智能抠图功能 (Smart Cutout)

#### 功能描述
比"去背景"更精细的物体提取，支持头发级精细抠图。

#### 优先级
中 ⭐⭐⭐⭐

#### 功能点
- 头发级精细抠图
- 多物体选择
- 边缘优化
- 半透明处理
- 智能边缘羽化

#### UI 设计
- 在工具列表中添加"智能抠图"卡片
- 图标：剪刀图标
- 上传图片
- 抠图模式：
  - 自动抠图（AI 自动识别主体）
  - 手动抠图（用户绘制选区）
  - 智能边缘（自动识别边缘）
- 边缘羽化滑块（0-20px）
- 保留细节滑块（低/中/高）
- 实时预览抠图效果

#### 交互行为
- 用户上传图片
- 选择抠图模式
- 自动抠图：AI 自动识别并抠取主体
- 手动抠图：使用画笔绘制选区，支持正选/反选
- 调整边缘羽化和保留细节
- 点击"完成"生成抠图结果
- 使用透明背景展示结果
- 支持更换背景（跳转到背景替换功能）
- 下载 PNG 格式（带透明通道）

#### 技术要点
- 使用 RMBG-1.4 或 SAM (Segment Anything Model) API
- 手动抠图：复用 `ImageMask` 组件
- 边缘羽化：Canvas feathering
- 实时预览：使用 `image-compare` 或透明叠加

#### API 端点
- `/vectorizer/api/v1/cutout` 或 `/302/submit/smart-cutout`
- 参数：image, mode, feather, preserve_detail

#### 多语言支持
- "智能抠图" / "Smart Cutout"
- 抠图模式支持国际化
- "更换背景" / "Replace Background"

#### 开发周期
1 周

---

### GIF 制作功能 (GIF Maker)

#### 功能描述
将多张图片或视频转换为 GIF 动图。

#### 优先级
中 ⭐⭐⭐

#### 功能点
- 多图转 GIF
- 视频转 GIF
- GIF 压缩
- 添加文字/贴纸
- 调整播放速度

#### UI 设计
- 在工具列表中添加"GIF 制作"卡片
- 图标：GIF 文件图标
- 输入模式选择：
  - 多图模式：上传多张图片
  - 视频模式：上传视频文件
- 图片列表：支持拖拽调整顺序
- GIF 设置：
  - 帧率（1-30 fps）
  - 尺寸（宽度 px）
  - 质量（低/中/高）
  - 循环次数（无限/自定义）
- 编辑功能：
  - 添加文字（字体、大小、颜色、位置）
  - 添加贴纸（预设贴纸库）
  - 裁剪 GIF 尺寸
- 预览播放器：循环预览 GIF

#### 交互行为
- 用户选择输入模式
- 上传图片或视频
- 调整图片顺序（多图模式）
- 设置 GIF 参数
- 添加文字或贴纸（可选）
- 点击"生成 GIF"
- 显示处理进度
- 生成完成后预览播放
- 调整参数后重新生成
- 下载 GIF 文件

#### 技术要点
- 使用 gif.js 或 ffmpeg.wasm 生成 GIF
- 视频转 GIF：提取视频帧
- 帧率控制：控制每帧显示时间
- 压缩：颜色量化 + 优化调色板
- 文字/贴纸：Canvas 绘制
- 预览播放：使用现有的 `VideoPlayer` 组件或 GIF 播放器

#### API 端点
前端处理为主，无需后端 API
或使用：`/302/submit/video-to-gif`

#### 多语言支持
- "GIF 制作" / "GIF Maker"
- 设置选项支持国际化

#### 开发周期
3-5 天

---

### 图片质量增强功能 (Image Quality Enhancement)

#### 功能描述
增强图片质量，包括去噪、锐化、亮度/对比度智能优化等。

#### 优先级
中 ⭐⭐⭐

#### 功能点
- 去噪
- 锐化
- 亮度智能优化
- 对比度智能优化
- 色彩校正
- 自动增强

#### UI 设计
- 在工具列表中添加"质量增强"卡片
- 图标：星星图标
- 上传图片
- 增强选项：
  - 去噪（滑块 0-100）
  - 锐化（滑块 0-100）
  - 亮度优化（滑块 -50 到 +50）
  - 对比度优化（滑块 -50 到 +50）
  - 色彩校正（开关）
- "一键增强"按钮：自动应用最佳参数
- 实时预览效果
- 对比模式：原图 vs 增强后

#### 交互行为
- 用户上传图片
- 手动调整各项增强参数
  或
- 点击"一键增强"自动应用最佳参数
- 实时预览增强效果
- 点击"确认"生成最终结果
- 展示对比效果
- 支持撤销/重做
- 下载或继续编辑

#### 技术要点
- 使用 Real-ESRGAN 或类似 API
- 自动增强：基于图像分析选择最佳参数
- 实时预览：使用 Debounce 防抖
- 参数组合：多个增强操作按顺序应用

#### API 端点
- `/302/submit/real-esrgan` 或 `/302/submit/quality-enhance`
- 参数：image, denoise, sharpen, brightness, contrast, color_correction

#### 多语言支持
- "质量增强" / "Quality Enhancement"
- 增强选项支持国际化
- "一键增强" / "Auto Enhance"

#### 开发周期
1 周

---

### 水印管理功能 (Watermark Management)

#### 功能描述
添加或去除图片水印。

#### 优先级
低 ⭐⭐⭐

#### 功能点
- 添加自定义水印（文字）
- 添加自定义水印（图片）
- 智能去除水印
- 批量添加水印
- 水印位置预设

#### UI 设计
- 在工具列表中添加"水印管理"卡片
- 图标：印章图标
- 功能切换：添加水印 / 去除水印

**添加水印模式：**
- 水印类型：文字 / 图片
- 文字水印：
  - 输入文本内容
  - 字体选择（预设字体列表）
  - 字体大小（滑块）
  - 字体颜色（颜色选择器）
  - 透明度（滑块 0-100）
- 图片水印：
  - 上传水印图片（支持 PNG 透明背景）
  - 缩放比例（滑块）
  - 透明度（滑块 0-100）
- 位置选择：
  - 预设位置：左上/右上/左下/右下/居中
  - 自定义位置：拖拽调整
- 平铺模式：重复水印（可选）

**去除水印模式：**
- 上传带水印的图片
- 手动标记水印区域（使用遮罩）
- 点击"去除水印"
- 显示处理进度
- 展示去除效果

#### 交互行为
- 用户选择功能模式
- 添加水印：
  - 设置水印内容和样式
  - 选择水印位置
  - 实时预览水印效果
  - 点击"应用"添加水印
- 去除水印：
  - 上传图片
  - 标记水印区域
  - 点击"去除水印"
  - 展示去除效果
- 下载或继续编辑

#### 技术要点
- 添加水印：Canvas 绘制文字/图片
- 去除水印：使用图像修复 API（inpaint）
- 实时预览：Canvas 渲染
- 位置计算：百分比定位，适配不同图片尺寸
- 平铺模式：循环绘制水印

#### API 端点
- 添加水印：前端处理，无需 API
- 去除水印：`/sd/v2beta/stable-image/edit/erase` (复用物体消除)

#### 多语言支持
- "水印管理" / "Watermark Management"
- "添加水印" / "Add Watermark"
- "去除水印" / "Remove Watermark"
- 位置选项支持国际化

#### 开发周期
2-3 天

---

### 证件照制作功能 (ID Photo Maker)

#### 功能描述
快速制作标准证件照，支持智能抠图、换底、尺寸裁剪。

#### 优先级
低 ⭐⭐⭐

#### 功能点
- 智能抠图（自动识别人像）
- 自动换底（红/蓝/白）
- 标准尺寸裁剪（1寸/2寸/小2寸等）
- 智能美颜
- 多规格导出（单张/排版）

#### UI 设计
- 在工具列表中添加"证件照制作"卡片
- 图标：证件图标
- 上传人像照片
- 尺寸选择：
  - 1寸（25mm x 35mm）
  - 2寸（35mm x 49mm）
  - 小2寸（35mm x 45mm）
  - 大1寸（33mm x 48mm）
  - 自定义尺寸
- 背景颜色选择：
  - 红色（标准）
  - 蓝色（标准）
  - 白色（标准）
  - 自定义颜色
- 美颜选项：
  - 磨皮（滑块）
  - 瘦脸（滑块）
  - 美白（滑块）
- 导出格式：
  - 单张照片
  - 排版照片（6寸相纸排版）

#### 交互行为
- 用户上传人像照片
- 系统自动识别人像并抠图
- 选择证件照尺寸
- 选择背景颜色
- 可选：调整美颜参数
- 实时预览证件照效果
- 点击"生成"制作证件照
- 展示单张和排版效果
- 下载单张或排版图片

#### 技术要点
- 智能抠图：复用"智能抠图"功能
- 换底：使用 `ImageMask` + Canvas 填充背景
- 尺寸裁剪：根据标准尺寸比例计算
- 排版：使用 Canvas 绘制多张照片（6寸相纸 152mm x 102mm）
- 美颜：复用"人像美化"功能

#### API 端点
- 组合现有功能，无需新 API
- 或使用：`/302/submit/id-photo-maker`

#### 多语言支持
- "证件照制作" / "ID Photo Maker"
- 尺寸名称支持国际化
- 背景颜色支持国际化

#### 开发周期
3-5 天

---

### 表情包制作功能 (Meme Maker)

#### 功能描述
基于人脸识别制作趣味表情包。

#### 优先级
低 ⭐⭐⭐

#### 功能点
- 人脸识别
- 表情模板匹配
- 添加文字气泡
- 动态表情包
- 表情包搜索（可选）

#### UI 设计
- 在工具列表中添加"表情包制作"卡片
- 图标：笑脸图标
- 上传人像照片
- 表情模板库：
  - 网格展示表情模板
  - 分类：搞笑/可爱/愤怒/惊讶等
  - 每个模板显示预览图
- 文字气泡：
  - 输入文字内容
  - 气泡样式（圆形/方形/云形）
  - 字体大小/颜色
- 动态效果：
  - 表情抖动
  - 放大缩小
  - 旋转
- 预览播放：实时预览表情包效果

#### 交互行为
- 用户上传人像照片
- 系统自动识别人脸
- 选择表情模板
- 人脸自动匹配到模板位置
- 添加文字气泡（可选）
- 选择动态效果（可选）
- 实时预览表情包
- 点击"生成"制作表情包
- 展示最终效果
- 下载 GIF 或静态图片

#### 技术要点
- 人脸识别：face-api.js 或后端 API
- 模板匹配：人脸关键点对齐
- 文字气泡：Canvas 绘制
- 动态效果：CSS 动画或 GIF 生成
- 使用 `gif.js` 生成动态表情包

#### API 端点
- 人脸识别：`/302/submit/face-detection`
- 前端处理为主

#### 多语言支持
- "表情包制作" / "Meme Maker"
- 表情分类支持国际化

#### 开发周期
1 周

---

## 新增功能开发优先级

| 功能     | 优先级   | 开发周期 | 商业价值  | 技术难度 |
|--------|-------|------|-------|------|
| 批量处理   | 🔥 最高 | 2-3周 | ⭐⭐⭐⭐⭐ | 中等   |
| 风格迁移   | 🔥 高  | 1-2周 | ⭐⭐⭐⭐⭐ | 中等   |
| 老照片修复  | 🔥 高  | 1周   | ⭐⭐⭐⭐  | 中等   |
| 人像美化   | 中     | 2周   | ⭐⭐⭐⭐  | 中等   |
| 智能抠图   | 中     | 1周   | ⭐⭐⭐⭐  | 中等   |
| GIF 制作 | 中     | 3-5天 | ⭐⭐⭐   | 低    |
| 图片质量增强 | 中     | 1周   | ⭐⭐⭐   | 中等   |
| 水印管理   | 低     | 2-3天 | ⭐⭐⭐   | 低    |
| 证件照制作  | 低     | 3-5天 | ⭐⭐⭐   | 低    |
| 表情包制作  | 低     | 1周   | ⭐⭐⭐   | 中等   |

---

## 推荐实施顺序

### 第一阶段（快速交付）
1. **水印管理** - 简单实用，开发周期短
2. **GIF 制作** - 利用现有能力，快速实现

### 第二阶段（功能扩展）
1. **风格迁移** - 创意工具，用户需求高
2. **老照片修复** - 完善老照片处理流程
3. **智能抠图** - 精细化处理工具

### 第三阶段（效率提升）
1. **批量处理** - 提升处理效率，商业价值最高

### 第四阶段（差异化竞争）
1. **人像美化** - 社交需求
2. **图片质量增强** - 完善图片处理能力
3. **证件照制作** - 实用工具
4. **表情包制作** - 娱乐功能

---

## 注意事项

本文档仅记录待开发的功能规划。
